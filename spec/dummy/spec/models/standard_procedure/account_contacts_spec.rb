require "rails_helper"

module StandardProcedure
  RSpec.describe Account, type: :model do
    describe "contacts" do
      it "adds a contact to the given organisation"
      it "adds a contact to the given organisation when given a organisation reference"
      it "removes a contact"
    end
  end
end
