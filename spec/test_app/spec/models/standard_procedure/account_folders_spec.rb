require "rails_helper"

module StandardProcedure
  RSpec.describe Account, type: :model do
    describe "folders" do
      it "adds a top-level folder"
      it "adds a sub-folder"
      it "removes a top-level folder"
      it "removes a sub-folder"
    end
  end
end
